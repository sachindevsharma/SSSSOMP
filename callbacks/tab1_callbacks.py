import dash
from dash import Input, Output, State
from dash.exceptions import PreventUpdate
import time


def callbacks_tab1():

   ''' @dash.callback(
            Output('number-counter', 'children'),
            Input('interval-component', 'n_intervals'),
            )
    def update_number(n):
        # print(n)
        print(1)
        while n <= 100:
            print(n)
            # time.sleep(delay)
            yield n + 1  # use a generator to yield each number
        else:
            PreventUpdate'''
    
   